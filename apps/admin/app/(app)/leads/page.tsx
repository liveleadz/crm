import { PageHeader, StubBody } from '@/components/page-header';

export default function LeadsPage() {
  return (
    <>
      <PageHeader title="Leads" subtitle="Kanban + table views" />
      <StubBody note="Drag-and-drop kanban, bulk actions, filters, and CSV import land in Sprint 1.3." />
    </>
  );
}
