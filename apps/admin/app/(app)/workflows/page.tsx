import { PageHeader, StubBody } from '@/components/page-header';

export default function WorkflowsPage() {
  return (
    <>
      <PageHeader title="Workflows" subtitle="Automation graphs" />
      <StubBody note="Visual graph editor + Edge Functions runner land in Sprint 4." />
    </>
  );
}
