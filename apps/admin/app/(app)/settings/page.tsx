import { PageHeader, StubBody } from '@/components/page-header';

export default function SettingsPage() {
  return (
    <>
      <PageHeader title="Settings" subtitle="Workspace and brand settings" />
      <StubBody note="Brand settings, stages, tags, and integrations land in Sprint 2." />
    </>
  );
}
